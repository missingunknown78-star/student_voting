[phases.setup]
nixPkgs = ["pango", "cairo", "gdk-pixbuf", "libffi", "libpng", "libjpeg", "zlib", "gobject-introspection", "glib"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --timeout 120"